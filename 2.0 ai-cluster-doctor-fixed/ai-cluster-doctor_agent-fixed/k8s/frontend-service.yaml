apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: ai-cluster-doctor
    component: frontend
spec:
  selector:
    app: ai-cluster-doctor
    component: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
